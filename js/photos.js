// PHOTOS view. STUB: replaced by the photo gallery / pinning UI.
export function mount(root) {
  root.innerHTML = `<div class="view-scroll"><div class="kicker">PHOTOS</div><p class="muted">Photo gallery coming online.</p></div>`;
}
export function unmount() {}
